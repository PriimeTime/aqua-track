import { StyleSheet } from "react-native";

const wsStyles = StyleSheet.create({
  wrapper: {
    alignItems: "center",
    justifyContent: "space-around",
    width: "50%",
    left: "25%",
    top: "25%",
    height: "50%",
  },
});

export { wsStyles };
