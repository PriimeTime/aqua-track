import { FONT_SIZE_11, FONT_SIZE_13, FONT_SIZE_16 } from "@/utils/constants";
import { mvs } from "react-native-size-matters";

const infoCardCurrentAmountHeight = mvs(25);
const infoCardCurrentAmountWidth = mvs(65);
const infoCardCurrentAmountRadius = mvs(10);
const infoCardCurrentAmountFontSize = FONT_SIZE_16;
const infoCardTotalAmountFontSize = mvs(FONT_SIZE_11);
const infoCardTotalAmountWidth = mvs(50);
const infoCardTotalAmountHeight = mvs(17.5);
const infoCardTotalAmountRadius = mvs(8);
const infoCardSizeTotalFontSize = FONT_SIZE_13;

export {
  infoCardCurrentAmountHeight,
  infoCardCurrentAmountWidth,
  infoCardCurrentAmountRadius,
  infoCardCurrentAmountFontSize,
  infoCardTotalAmountFontSize,
  infoCardTotalAmountWidth,
  infoCardTotalAmountHeight,
  infoCardTotalAmountRadius,
  infoCardSizeTotalFontSize,
};
