import { FONT_SIZE_24 } from "@/utils/constants";
import { ms } from "react-native-size-matters";

const actionModalFontSize = ms(FONT_SIZE_24);
const actionModalRadius = ms(30);

export { actionModalFontSize, actionModalRadius };
